<UserControl x:Class="OperationLogManager.Views.OperationLog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OperationLogManager.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:RegionManage="clr-namespace:SharedResource.libs;assembly=SharedResource"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <materialDesign:Card>
            <TabControl HorizontalContentAlignment="Left"
                        materialDesign:ColorZoneAssist.Background=" #7CA000"
                        materialDesign:ColorZoneAssist.Mode="SecondaryMid"
                        Height="auto"
                        Style="{StaticResource MaterialDesignFilledTabControl}">
                <TabItem Header="运行日志" Padding="0" Height="40">
                    <ContentControl prism:RegionManager.RegionName="{x:Static RegionManage:RegionManage.NormalLog_Region}"/>                    
                </TabItem>
                <TabItem Header="异常日志" Padding="0" Height="40">
                    <ContentControl prism:RegionManager.RegionName="{x:Static RegionManage:RegionManage.ExceptionLog_Region}"/>
                </TabItem>
            </TabControl>
        </materialDesign:Card>
    </Grid>
</UserControl>
