<UserControl x:Class="OperationLogManager.Views.ExceptionLog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OperationLogManager.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid ItemsSource="{Binding ExceptionLogs}"
                  AutoGenerateColumns="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  Margin="8"
                  IsReadOnly="True">
            <DataGrid.Resources>
                <!-- 显示时换行 -->
                <Style x:Key="WrapTextBlockStyle" TargetType="TextBlock">
                    <Setter Property="TextWrapping" Value="Wrap"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>

                <!-- 编辑时换行 -->
                <Style x:Key="WrapTextBoxStyle" TargetType="TextBox">
                    <Setter Property="TextWrapping" Value="Wrap"/>
                    <Setter Property="AcceptsReturn" Value="True"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="时间" Binding="{Binding Time}" Width="1.3*"/>
                <DataGridTextColumn Header="级别" Binding="{Binding Level}" Width="70"/>
                <DataGridTextColumn Header="消息" Binding="{Binding Message}" Width="1.75*"/>
                <DataGridTextColumn Header="异常详情" Binding="{Binding Exception}" Width="3.25*"
                    ElementStyle="{StaticResource WrapTextBlockStyle}"
        EditingElementStyle="{StaticResource WrapTextBoxStyle}">
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
