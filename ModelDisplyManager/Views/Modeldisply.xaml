<UserControl x:Class="ModelDisplyManager.Views.Modeldisply"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModelDisplyManager.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:hx="http://helix-toolkit.org/wpf/SharpDX"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>            
            <hx:Viewport3DX
                x:Name="view"
                Camera="{Binding Camera}"
                EffectsManager="{Binding EffectsManager}"
                CameraMode="Inspect"
                CameraRotationMode="Turnball"
                Background="White"
                BackgroundColor="#B3FFFFFF"
                CoordinateSystemLabelForeground="White"
                EnableDesignModeRendering="False"
                EnableSwapChainRendering="False"
                ShowCoordinateSystem="False"
                ShowViewCube="False"
                TextBrush="Black"
                FXAALevel="High"
                
                IsHitTestVisible="False"
                IsManipulationEnabled="False">
                <!--<hx:Viewport3DX.InputBindings>
                    <MouseBinding Command="hx:ViewportCommands.Rotate" Gesture="RightClick"/>
                    <MouseBinding Command="hx:ViewportCommands.Zoom" Gesture="MiddleClick"/>
                    <MouseBinding Command="hx:ViewportCommands.Pan" Gesture="LeftClick"/>
                </hx:Viewport3DX.InputBindings>-->
                <hx:AmbientLight3D Color="#363636"/>
                <hx:DirectionalLight3D Color="#d6d6d6" Direction="{Binding Camera.LookDirection}"/>
                <hx:GroupModel3D ItemsSource="{Binding QZC_STLmodel}"/>
            </hx:Viewport3DX>
            <!--<StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                <Button Command="{Binding StarttestCommand}">启动动画</Button>
                <Button Command="{Binding StoptestCommand}">停止动画</Button>
            </StackPanel>-->
        </Grid>        
    </Grid>
</UserControl>
