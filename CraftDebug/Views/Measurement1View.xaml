<UserControl x:Class="CraftDebug.Views.Measurement1View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CraftDebug.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:converter="clr-namespace:CraftDebug.converter"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converter:RowParamConverter x:Key="RowParamConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid>
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0 5 0 1">
                    <Button Content="添加测量次数" Command="{Binding AddRowCommand}" Margin="5 1 5 1"/>
                    <Button Content="清除" Command="{Binding ClearContentsCommand}" Margin="5 1 5 1"/>
                    <Button Content="导出" Command="{Binding OutputCommand}" CommandParameter="1" Margin="5 1 5 1"/>
                </StackPanel>

                <Border>
                    <DataGrid ItemsSource="{Binding Measurements}"
                              VirtualizingStackPanel.IsVirtualizing="False"
    AutoGenerateColumns="False"
   SelectionMode="Single"
   SelectionUnit="FullRow"
   HeadersVisibility="Column"
   CanUserAddRows="False"
   CanUserDeleteRows="False"
   VerticalAlignment="Stretch"
   HorizontalAlignment="Stretch"
   Background="White"
   BorderBrush="LightGray"                          
   BorderThickness="1"  Margin="-4 4 0 -4" 
   Height="270"
   VerticalScrollBarVisibility="Hidden"
   HorizontalScrollBarVisibility="Hidden">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="次数">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Index, StringFormat=第{0}次}" 
                                           Width="50"
                                           VerticalAlignment="Center" 
                                           HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="A">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBox Text="{Binding PointA, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue='', FallbackValue=''}" Width="100" VerticalAlignment="Center"/>
                                            <Button Content="读取"
                                            Command="{Binding DataContext.FillPointCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, 
                                        Converter={StaticResource RowParamConverter}, ConverterParameter=1}"
                                            Margin="5,0,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="B">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBox Text="{Binding PointB, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue='', FallbackValue='0'}" Width="100" VerticalAlignment="Center"/>
                                            <Button Content="读取"
         Command="{Binding DataContext.FillPointCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
         CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, 
             Converter={StaticResource RowParamConverter}, ConverterParameter=2}"
         Margin="5,0,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>                            

                            <DataGridTemplateColumn Header="差值">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Difference, TargetNullValue='', FallbackValue='0'}" Width="100" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
