<UserControl x:Class="DeviceMegManager.Views.ProcessProgress"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeviceMegManager.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <ProgressBar Height="10" ToolTip="{Binding Progress, StringFormat={}{0:F3}}" Value="{Binding Progress}">
            <ProgressBar.Foreground>
                <SolidColorBrush Color="#6B8500"/>
            </ProgressBar.Foreground>
        </ProgressBar>

        <TextBlock Grid.Column="1"
                   Margin="3 0"
                   FontSize="12"
                   VerticalAlignment="Center"
                   Text="{Binding Number}"></TextBlock>
    </Grid>
</UserControl>
